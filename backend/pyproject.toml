[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["migrations", "__pycache__", ".venv", "venv"]

[tool.ruff.lint]
select = ["E", "F", "W"]
# E701/E702/E712/F401 はフェーズ3で解消済み。
# E501 のみ意図的に許容している（日本語のAIプロンプトテンプレート文字列は
# 意図的に長い行が多く、折り返すとかえって可読性が落ちるため）。
ignore = [
    "E501",
]
