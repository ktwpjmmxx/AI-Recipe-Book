# -*- coding: utf-8 -*-
# 開発・テスト時のみ必要な追加依存
# 使い方: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
httpx==0.28.1          # FastAPI TestClient が内部で使用
                       # google-genai==2.11.0 が httpx>=0.28.1 を要求するため引き上げ
ruff==0.15.20           # lintチェック（CIで使用）
